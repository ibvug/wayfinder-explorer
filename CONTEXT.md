# Wayfinder Explorer

Wayfinder Explorer 是受 Wayfinder 启发、但拥有自身领域语义的目标探索领域。它帮助探索者在不确定性中逐步找到通往目的地的路径，而不是展示或执行一条预先完整的路线。

## Language

**目标探索**:
从模糊意图出发，通过逐个解决决策问题，使通往目的地的路径逐渐清晰的过程。
_Avoid_: 完整路线浏览、预制决策树推进

**Wayfinder Explorer**:
受 Wayfinder 启发、但不以兼容或忠实复现 Wayfinder 规则为目标的交互式目标探索产品。
_Avoid_: Wayfinder 客户端、Wayfinder 可视化外壳、节点图查看器、任务流程图

**地图 Agent**:
贯穿一次目标探索、负责形成首张地图和后续地图变化提案的逻辑角色。它协调新答案对整张图的影响，但不能直接修改确认状态或替探索者作出真实判断。
_Avoid_: 地图视图、地图文件写入器、探索 Agent

**探索 Agent**:
围绕一个已认领议题与探索者持续对话、取证并形成答案草案的逻辑角色。它只向 Explorer 返回对话结果和提案，不能直接新增、修改或删除地图内容。
_Avoid_: 地图 Agent、地图文件写入器、一次性问答

**Agent 会话**:
Explorer 为一个地图 Agent 或探索 Agent 保存的稳定、可恢复的持续对话身份。它能够在权限边界内读取文件、调用工具并观察当前环境，涉及写入或其他副作用时必须取得相应授权。地图 Agent 会话贯穿整次目标探索，探索 Agent 会话绑定一个已认领议题；底层任务运行时可以替换而不改变会话的领域身份、历史和能力边界。
_Avoid_: 单轮消息、运行时任务 ID、临时模型调用、静态证据问答

**绘制地图**:
地图 Agent 主持的目标探索起始阶段，从模糊目标中确认目的地，并记录当前能够明确表达的议题、迷雾和范围边界。
_Avoid_: 生成完整路线、预先拆完所有后续问题

**推进地图**:
地图建立后的探索阶段，每次解决一个当前可探索议题，再由地图 Agent 根据新认识协调受影响内容并重新确定地图前沿。
_Avoid_: 按预制节点顺序执行、只改变节点状态、探索者手工维护地图

**当前前沿**:
所有未解决、未被阻塞且未被认领的议题组成的当前可探索集合。当前选中的议题只是其中一个，不代表其他前沿议题消失。
_Avoid_: 当前节点、下一步节点

**认领**:
探索者明确开始处理议题时取得的排他工作权。查看议题摘要不会认领；认领成功后其他会话不能重复开始该议题。确认答案或由认领者主动结束探索时，认领随之结束。
_Avoid_: 选择、查看

**当前选中状态**:
探索者此刻正在查看的一个节点或议题。当前选中只影响界面显示，不会认领议题或改变地图。
_Avoid_: 当前节点、正在探索、已确认

**正在探索状态**:
已经认领并拥有持续探索会话的议题状态。多个议题可以并行处于正在探索，每个探索各自处理自己的议题，且不会因无关答案的确认而被中断或改写。它们仍不是地图节点，也不会形成确定路线；只有认领者可以确认答案或主动结束探索。相关前提改变时，地图 Agent 把变化交给对应探索 Agent，在原会话中协调影响并重新形成可确认内容，不要求探索者手工校准会话、草案或地图。
_Avoid_: 当前选中、已完成

**结束探索**:
认领者明确停止一个正在探索的议题、释放认领并将它移出本次目标探索范围的行为。探索会话及已有内容进入未完成历史，不形成确认答案、地图节点或确定路线；重新绘图不能替认领者结束探索。
_Avoid_: 确认答案、切换当前选中、后台自动清理

**未完成历史**:
由认领者主动结束、但没有形成确认答案的探索记录。它保留原议题、会话和已有内容，但不再属于本次探索范围或开放议题，因此不会阻止抵达目的地。
_Avoid_: 答案草案、未探索历史、已确认节点

**地图**:
由起点、目的地以及曾经确认后进入地图的节点和连接构成的一张持续演化的图。新议题在答案确认前不进入地图；已经进入地图的节点即使因前提变化而待复核，也保留在原图中的身份和位置，但不再被表现为当前确定事实或确定路线。
_Avoid_: 议题库存、完整依赖图、每次变化生成的新图

**议题**:
等待探索并形成答案的明确问题。一个议题覆盖一次连贯探索能够解决的完整自然决策，可以在内部包含“如果需要，再选择方案”之类的条件分支；不能只为制造依赖而把同一个自然决策拆成多个议题。只有后续问题需要独立探索或新的事实时，才在条件明确后生成新议题。议题在答案得到确认前不是地图节点。
_Avoid_: 未完成节点、候选节点

**展开的议题**:
用户点击地图节点后显示的待解决议题。它可以进入当前选中状态或正在探索状态，但完成并确认前仍然只是议题，不属于地图节点或确定路线。
_Avoid_: 已完成节点

**答案草案**:
探索 Agent 在当前前提下通过探索对话形成、等待探索者审阅的候选答案。答案草案可以继续修改或暂存，但不会解决议题或生成地图节点；相关前提改变时，地图 Agent 协调对应探索 Agent 保留旧草案为历史并重新评估，只向探索者呈现协调后的可确认草案或无法推断的真实冲突。
_Avoid_: 最终答案、自动决定、要求探索者手工同步

**确认答案**:
探索者明确接受答案草案，使议题成为已解决事实并在地图上显示对应节点的行为。确认答案随后触发影响协调和必要的重新绘图；多个答案接近同时确认时，按确认顺序逐个处理，后一次协调以包含此前确认结果的最新地图为基础，但不会替探索者改动后一个答案中的真实判断。
_Avoid_: 生成草案、结束对话、离开页面

**修订答案**:
新认识表明已确认答案不再支持抵达目的地时，探索者在同一次目标探索中显式确认替代答案的行为。旧答案保留为历史，但不再代表当前答案；替代答案成为新的已解决事实并触发影响协调。地图 Agent 负责找出并更新所有可推导的后续影响，只把无法可靠推断的真实决策冲突交还给探索者。
_Avoid_: 原地覆盖、只能建立新目标探索、修改答案草案

**影响协调**:
地图 Agent 在答案确认或修订后，根据新语义维护整次目标探索一致性的过程。地图 Agent 自行分析显式依赖和语义关联，更新可推导的议题、关系、路线、探索上下文和答案草案；只有相互冲突的探索者判断无法从现有意图可靠推断时，才形成一个需要探索者回答的复核问题。
_Avoid_: 探索者逐项维护、只检查显式依赖、把机械变化逐个交还确认

**待复核答案**:
影响协调发现与新前提存在真实冲突、且地图 Agent 无法从探索者已有意图可靠解决的既有确认答案。它保留原答案、历史以及同一个地图节点，但复核完成前不再作为当前已解决事实，相关连接也不再属于确定路线，并会阻止抵达目的地；可推导的后续变化由地图 Agent 直接协调，不会全部变成需要探索者逐个处理的待复核答案。
_Avoid_: 所有下游答案、自动删除、普通开放议题

**复核答案**:
探索者对地图 Agent 无法可靠推断的真实冲突作出判断的行为。原答案仍成立时，同一个地图节点恢复为当前已解决事实；不再成立时则修订答案。探索者只回答冲突本身，地图 Agent 随后协调所有可推导的图和探索变化，不要求探索者逐项修改。
_Avoid_: 手工修改节点、重新创建节点、逐文件同步

**议题依赖**:
一个议题进入当前前沿前必须完成的其他议题。议题答案得到确认后，这些依赖关系成为地图中的确定路线。
_Avoid_: 来源节点、视觉父节点

**地图节点**:
地图中已经显示并保持身份连续的点，包括起点节点、完成议题后显示的节点和目的地节点。已进入地图的节点在待复核时仍是同一个地图节点；当前选中或正在探索的新议题不会因此成为地图节点。
_Avoid_: 待解决问题、被阻塞议题

**确定路线**:
当前有效的已确认答案所支持的地图节点连接。与待复核答案相关的连接在复核完成前不属于确定路线；起点与目的地不会因为同时存在就自动形成路线。
_Avoid_: 计划路线、议题依赖线、预测路径

**起点节点**:
绘制地图前确认的固定现状基线，说明目标探索是从什么基础出发，例如从零规划或基于现有项目、分支和能力继续改变。后续环境变化不会自动改写这个节点。
_Avoid_: 第一个议题、默认空白状态

**取证范围**:
在检查现有环境前与探索者共同确定的事实边界，限定 Agent 会话可以读取和使用工具检查的项目、分支、资料或其他来源。取证范围允许观察事实，但不自动授予写入或其他副作用权限。
_Avoid_: 全量扫描、盲目查看、隐含写入授权

**目的地节点**:
绘制地图前确认的目标状态，描述这次目标探索最终需要抵达的可观察结果。地图创建后目的地保持稳定；如果抵达标准发生根本变化，应建立新的目标探索。
_Avoid_: 最后一个任务、预设解决方案

**抵达目的地**:
目标探索的决策路径已经闭合，而不是现实工作已经执行完成。成功重新绘图后，范围内不能再有任何开放议题或尚未明确的迷雾：已经认领、正在探索和仍被阻塞的议题都属于开放议题，都会阻止抵达；其余内容必须已经形成确认答案，或明确退出本次探索范围并关闭。地图处于待重新绘图状态时不能判定抵达。
_Avoid_: 现实交付完成、用户任意宣布结束、只完成一条分支

**重新绘图**:
地图 Agent 在影响协调中根据新答案梳理尚未开始探索的议题，创建、调整或标记需要删除的议题，并重新确定当前前沿的过程。正在探索的议题保持身份、会话和认领；地图 Agent 将相关前提变化交给对应探索 Agent，在原会话中协调草案。其他没有变化的内容保持原样，修改默认采用新内容并允许用户恢复旧内容。需要删除的议题先标记为待删除，并在下一次重新绘图时根据最新答案再次评估，只有仍无必要时才真正删除。判定抵达前，最后一次成功重新绘图必须使所有范围内议题得到确认或关闭，并清空尚未明确的迷雾；无必要的未开始议题退出活跃地图并进入未探索历史。
_Avoid_: 探索者手工改图、点击时生成问题、重新计算视觉布局

**恢复议题**:
探索者撤销最近一次重新绘图对某个尚未开始探索议题所作的修改或删除，使该议题恢复到变化前的内容。恢复只撤销本次变化，不会认领、长期保护或永久锁定议题；以后的重新绘图仍可重新评估它。
_Avoid_: 开始探索、永久保留、确认答案

**未探索历史**:
重新绘图确认对抵达已经没有必要、并在判定抵达前关闭的未开始议题记录。这些议题从本次探索范围、活跃地图和当前前沿中移除，但作为本次目标探索曾经考虑过的分支保留；它们不再属于开放议题。
_Avoid_: 待删除议题、当前前沿、彻底删除

**待重新绘图状态**:
答案已经确认，但由它触发的重新绘图尚未成功完成的状态。地图立即显示新确认的节点，其余部分保持上次成功重新绘图的结果，并明确表明当前前沿尚未更新。已确认答案不会被回滚；已有探索可以继续，但不能从旧前沿开始新探索，后续答案确认也会暂停。系统会自动重试并允许探索者立即重试，成功后恢复开始入口并继续确认队列。
_Avoid_: 未确认、确认失败、答案草案
