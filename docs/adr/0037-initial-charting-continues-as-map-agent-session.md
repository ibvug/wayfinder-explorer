# 首图绘制会话继续承担整次地图维护

每次目标探索只有一个逻辑地图 Agent 会话：它从目的地确认和首图绘制开始，在首图创建后仍保留稳定身份，并在答案确认或修订后继续形成地图变化提案。当前 Codex 运行时将该会话绑定到一个可恢复的持久 Codex Thread，而不是在每次重新绘图时创建互不相关的新任务。Explorer 外置保存地图真源、会话身份和运行时绑定，因此 Codex 进程重启或未来更换 Agent 运行时都不会改变这次目标探索的身份与历史。
