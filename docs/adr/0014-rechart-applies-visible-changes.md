# 重新绘图自动应用并显示变化

重新绘图不增加一次整体确认，并且不会修改或删除正在探索的议题。对于其他议题，没有变化的旧内容保持原样；需要修改的议题默认采用新内容，同时显示修改内容，用户可以通过交互恢复原议题；需要删除的议题本轮只显示为淡色、逐渐消失的状态，用户可以恢复。下一次重新绘图会根据最新答案再次评估待删除议题，只有仍无必要时才真正删除，重新相关则自动保留或调整。恢复只撤销本次变化，不会长期保护议题，以后的重新绘图仍可重新评估它。这样变化默认向前推进，同时给用户一轮重新绘图的纠正窗口。
