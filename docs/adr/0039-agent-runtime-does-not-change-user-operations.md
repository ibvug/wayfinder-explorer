# Agent 运行时抽象不改变既有用户操作

把逻辑 Agent 会话绑定到 Codex Thread 或未来其他运行时，只是 Explorer 内部结构变化，不能改变探索者已经使用的地图交互和确认流程。开始绘图、开始探索、发送回答、形成草案、继续讨论、暂存草案、预览地图变化和确认写回仍保持原有含义与入口。专用地图修改能力只在内部把 Agent 提案转换为经过版本校验的预览和原子写入；它不增加一套新的用户操作，也不能用普通工具审批替代既有的地图预览与明确确认。未来更换 Agent 运行时也必须满足同一用户操作契约。
