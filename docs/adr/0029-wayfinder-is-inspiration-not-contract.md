# Wayfinder 提供启发而非语义契约

Wayfinder Explorer 借鉴 Wayfinder 的目标探索思想，但拥有自己的领域语义，不以兼容或忠实复现 Wayfinder 的规则为目标。Explorer 的议题、路线、重新绘图和完成语义应根据自身用户体验与领域一致性决定；Wayfinder 中的既有做法是设计输入而不是必须服从的约束。这样可以避免交互式可视化产品被原型的既有形态限制，同时要求所有有意偏离都在 Explorer 自己的模型中明确成立。
