# 并行探索的答案按确认顺序重新绘图

多个正在探索的议题可以各自形成答案草案并请求确认，但每个确认从探索者明确接受草案、答案成为地图中的已解决事实，到影响协调和必要的重新绘图完成，属于一个按确认顺序严格串行、不能彼此交错的完整生命周期。后一确认在前一项协调成功前不得落图；如果前一项失败，失败状态必须先完整持久化为可恢复且不会与后续变化交错的稳定边界，但这不会解锁后一确认，确认队列仍须暂停并先恢复失败项。后续重新绘图也不能覆盖先发生的变化。相关的正在探索状态可以依 ADR-0035 在原会话中接收前提、上下文和答案草案协调，但这些协调不会自动替探索者改变其中的真实判断。
